FROM node:22-alpine

WORKDIR /app

RUN apk add --no-cache libc6-compat python3 make g++

COPY package.json package-lock.json* ./
RUN npm ci

# سورس کامل را از ایمیج نمی‌آوریم؛ روی سرور با Volume از /root/fixbazi می‌آید
EXPOSE 3000

CMD ["npm", "run", "dev"]
